import type { IngestMetrics } from "./ingest";
export interface ListeningServer {
    port: number;
    close(): Promise<void>;
}
export declare class Prometheus implements IngestMetrics {
    private chainHeightValue;
    private ingestSpeedValue;
    private registry;
    private lastBlock;
    private chainHeight;
    private mappingSpeed;
    private ingestSpeed;
    private syncETA;
    private syncRatio;
    constructor();
    setLastProcessedBlock(height: number): void;
    setChainHeight(height: number): void;
    getChainHeight(): number;
    setMappingSpeed(blocksPerSecond: number): void;
    setIngestSpeed(blocksPerSecond: number): void;
    getIngestSpeed(): number;
    setSyncETA(seconds: number): void;
    setSyncRatio(ratio: number): void;
    private handleHttpRequest;
    serve(port: number | string): Promise<ListeningServer>;
}
//# sourceMappingURL=prometheus.d.ts.map
export interface Service {
    close(): void | Promise<void | Error>;
}
export declare class ServiceManager {
    private services;
    private stopped;
    add<T extends Service>(s: T): T;
    private stop;
    private shutdown;
    static run(app: (sm: ServiceManager) => Promise<void>): void;
}
//# sourceMappingURL=sm.d.ts.map
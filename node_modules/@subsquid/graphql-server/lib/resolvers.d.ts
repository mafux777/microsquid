import { DocumentNode } from "graphql";
import { ResolversMap } from "type-graphql";
import type { EntityManager } from "typeorm";
export interface CustomResolvers {
    typeDefs: DocumentNode;
    resolvers: ResolversMap;
}
export declare function loadCustomResolvers(mod: string): Promise<CustomResolvers>;
export interface CustomResolverClass {
    new (tx: () => Promise<EntityManager>): this;
}
//# sourceMappingURL=resolvers.d.ts.map
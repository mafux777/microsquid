import { ChainDescription, Ti } from "@subsquid/substrate-metadata";
import { Output } from "@subsquid/util";
export declare class Interfaces {
    private types;
    private nameAssignment;
    private assignedNames;
    private generated;
    private generatedNames;
    private queue;
    constructor(description: ChainDescription);
    use(ti: Ti): string;
    private makeType;
    makeTuple(fields: Ti[]): string;
    private makeStruct;
    private printStructFields;
    private makeVariant;
    private getName;
    isEmpty(): boolean;
    generate(out: Output): void;
    qualify(ns: string, typeExp: string): string;
}
//# sourceMappingURL=ifs.d.ts.map
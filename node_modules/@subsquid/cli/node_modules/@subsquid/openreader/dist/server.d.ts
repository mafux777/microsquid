/// <reference types="node" />
import { ApolloServer } from "apollo-server-express";
import express from "express";
import http from "http";
import type { Pool } from "pg";
import { Dialect } from "./dialect";
import type { Model } from "./model";
export interface ListeningServer {
    readonly port: number;
    stop(): Promise<void>;
}
export interface ServerOptions {
    model: Model;
    db: Pool;
    port: number | string;
    dialect?: Dialect;
    graphiqlConsole?: boolean;
}
export declare function serve(options: ServerOptions): Promise<ListeningServer>;
export declare function listen(apollo: ApolloServer, server: http.Server, port: number | string): Promise<ListeningServer>;
export declare function setupGraphiqlConsole(app: express.Application): void;
//# sourceMappingURL=server.d.ts.map
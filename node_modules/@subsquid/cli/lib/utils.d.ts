import { Command } from '@oclif/core';
import { SimpleGit } from 'simple-git';
export declare function pollDeployPipelines(squidName: string, versionName: string, deploymentUrl: string, command: Command): Promise<void>;
export declare function buildRemoteUrlFromGit(git: SimpleGit, command: Command): Promise<string>;
export declare function parseNameAndVersion(nameAndVersion: string, command: Command): {
    squidName: string;
    versionName: string;
};
//# sourceMappingURL=utils.d.ts.map
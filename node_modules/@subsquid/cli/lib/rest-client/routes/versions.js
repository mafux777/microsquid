"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.versionList = void 0;
const baseUrl_1 = require("../baseUrl");
const creds_1 = require("../../creds");
const request_1 = require("../request");
async function versionList(squidName) {
    const apiUrl = `${baseUrl_1.baseUrl}/client/squid/${squidName}/versions`;
    const response = await (0, request_1.request)(apiUrl, {
        method: 'get',
        headers: {
            // eslint-disable-next-line @typescript-eslint/naming-convention
            'Content-Type': 'application/json',
            authorization: `token ${(0, creds_1.getCreds)()}`,
        },
    });
    const responseBody = await response.json();
    if (response.status === 200) {
        return responseBody;
    }
}
exports.versionList = versionList;
//# sourceMappingURL=versions.js.map
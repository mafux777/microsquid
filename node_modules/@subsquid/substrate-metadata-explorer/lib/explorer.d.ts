import { Log, SpecVersion } from "./types";
export interface Version {
    specVersion: SpecVersion;
    blockNumber: number;
    blockHash: string;
}
export declare class Explorer {
    private first;
    private last;
    private fetch;
    private log?;
    private queue;
    private versions;
    private constructor();
    private add;
    private explore;
    static getVersions(firstBlock: number, lastBlock: number, fetch: (heights: number[]) => Promise<Version[]>, log?: Log): Promise<Version[]>;
}
//# sourceMappingURL=explorer.d.ts.map